{:port 4000
 :mode :development
 :database { :host "127.0.0.1"
             :port 3306
             :name "{{sanitized}}"
             :user "{{sanitized}}"
             :password "{{sanitized}}"}
}

